{
  "name": "progressive-workflow",
  "version": "1.0.0",
  "description": "Execute pre-defined workflows step by step for Claude Code",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf ../../plugins/progressive-workflow/skills/progressive-workflow/scripts/*.js",
    "prebuild": "npm run clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
